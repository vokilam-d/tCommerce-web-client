import { NgUnsubscribe } from './ng-unsubscribe.directive';

describe('NgUnsubscribe', () => {
  it('should create an instance', () => {
    const directive = new NgUnsubscribe();
    expect(directive).toBeTruthy();
  });
});
