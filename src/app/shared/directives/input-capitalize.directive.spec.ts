import { InputCapitalizeDirective } from './input-capitalize.directive';

describe('InputCapitalizeDirective', () => {
  it('should create an instance', () => {
    const directive = new InputCapitalizeDirective();
    expect(directive).toBeTruthy();
  });
});
