import { FlyToCartDirective } from './fly-to-cart.directive';

describe('FlyToCartDirective', () => {
  it('should create an instance', () => {
    const directive = new FlyToCartDirective(null);
    expect(directive).toBeTruthy();
  });
});
